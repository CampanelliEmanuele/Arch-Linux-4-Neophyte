!!! First u need to connect to a Wifi connection !!!
!!! First u need to connect to a Wifi connection !!!
!!! First u need to connect to a Wifi connection !!!

Run these commands:
  sudo pacman -Syyu     (update system)
  sudo pacman -S xorg   (Install a display server)

Git installation:
  cd
  sudo pacman -S git

Dopodiché eseguire i seguenti comandi:
  cd
  clear
  sudo systemctl enable NetworkManager --now

Ora bisogna devi copiare ed incolare il file "auto-install" in una cartella, per poi eseguire i comandi:
  chmod +x auto-install.sh
  ./auto-install.sh
