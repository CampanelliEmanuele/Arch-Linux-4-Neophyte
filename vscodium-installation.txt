1) Installation using AUR Helper:-
  using Yay:-
    yay vscodium-bin

  using paru
    paru -S vscodium-bin

2) Installation using git(by Building VS Code from Source):-
  sudo pacman -S git base-devel
  git clone https://aur.archlinux.org/vscodium-bi...
  cd vscodium-bin
  makepkg -si
